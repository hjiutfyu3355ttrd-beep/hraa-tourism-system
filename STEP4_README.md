# خطوة 4 — كشف ربحية الرحلة (trip_statement.html)

## اللي اتعمل

### 1) إصلاح مهم في `getTripFinancials()` (supabase.js)
كانت الدالة بتحسب صافي الربح كـ: `المُحصّل − المصروفات` فقط، من غير ما تخصم
عمولة المندوب رغم إن كل حجز مرتبط بمندوب وله `commission_rate`. تم إضافة:
- حساب عمولة كل حجز = `booking_value × commission_rate ٪` لكل مندوب مرتبط بحجوزات الرحلة.
- تجميعها في `byRepresentative` (تفصيل لكل مندوب) و`totalCommission` (الإجمالي).
- **صافي الربح الجديد = المُحصّل − المصروفات − عمولات المناديب**، وأضيف `profitMargin`
  (هامش الربح %).

⚠️ ده معناه إن أي مكان تاني في النظام بيستخدم `getTripFinancials()` (`trips.html`،
`trip_details.html`) هيعرض دلوقتي رقم ربح أصح (أقل من الرقم القديم لو فيه عمولات).

### 2) صفحة جديدة: `trip_statement.html`
كشف ربحية مفصّل لكل رحلة، بنفس نمط `client_statement.html`/`supplier_statement.html`
(قابل للطباعة عبر Ctrl+P أو زرار طباعة)، فيه:
- اختيار الرحلة (أو فتحها مباشرة برابط `trip_statement.html?trip_id=...`).
- بطاقات ملخص: قيمة الحجوزات، المُحصّل، التكاليف، عمولات المناديب، صافي الربح،
  هامش الربح %، نسبة الإشغال (عدد الحجوزات / seats_total).
- جدول تفصيل الحجوزات (كود الحجز، العميل، المندوب، القيمة، المُحصّل، المتبقي، الحالة).
- جدول التكاليف حسب التصنيف.
- جدول عمولات المناديب (يظهر بس لو فيه مناديب مرتبطين بحجوزات الرحلة).

### 3) الربط من باقي الصفحات
- زرار جديد 📈 في صف كل رحلة بجدول `trips.html`.
- زرار "كشف الربحية" في أعلى `trip_details.html`.
- رابط "كشف ربحية الرحلة" في قائمة التقارير بالـ sidebar في كل صفحات النظام.

## ملاحظة
حساب العمولة مبني على `booking_value` (قيمة الحجز الكاملة) وليس على المبلغ
المُحصّل فعليًا — ده الأساس المعتاد لعمولة المبيعات. لو إنت عايزها تُحسب على
المُحصّل بس (نظام عمولة على أساس نقدي)، قولّي وأعدّل المعادلة بسهولة.
