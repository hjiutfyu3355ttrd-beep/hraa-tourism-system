# خطوة 4 (تكملة) — معاملات treasury.html اليدوية بقت قيود محاسبية حقيقية

## القرار
اتفقنا نمشي على نفس المبدأ المتبع في أي نظام محاسبي: **كل حركة نقدية،
مهما كانت بسيطة، لازم تعدّي من المحرك المحاسبي المزدوج (قيد يومية متوازن)
مش تتسجل في جدول منفصل بجانبه.** ده اللي كان ناقص في زرار "وارد/صادر" و"رصيد
افتتاحي" في `treasury.html` — كانوا بيسجلوا في جدول `transactions` القديم
بس، فرصيد الخزينة المعروض في الصفحة كان ممكن ينفصل بصمت عن `getTrialBalance()`
(بالظبط نفس مشكلة تحويل البنك اللي اتصلحت في خطوة 6).

## اللي اتعمل

### 1) SQL (`accounting_schema_step4.sql`) — شغّله في Supabase أولاً
- حسابان جديدان في دليل الحسابات مكانوش موجودين: `4900` (إيرادات أخرى) و
  `5800` (مصروفات سيارات) — عشان فئات "سيارة"/"شركة"/"بنك" في نافذة
  الإضافة تلاقي لها حساب محاسبي مناسب بدل ما تتحط كلها تحت حساب واحد عام.
- عمود جديد `linked_voucher_id` على جدول `transactions` — بيربط كل صف
  قديم بالسند المحاسبي المقابل له، عشان التعديل/الحذف يقدروا يعكسوا القيد
  الصح بدل ما يسيبوه يتيم في ميزان المراجعة.

### 2) `treasury.html`
- `addTransactionToSupabase()` و`addOpeningBalance()`: بقوا بينادوا
  `Supabase.addVoucher()` (نفس دالة `vouchers.html` بالظبط) قبل ما يسجّلوا
  في `transactions` — يعني كل وارد/صادر يدوي بقى له سند حقيقي وقيد متوازن:
  - "عميل" → الطرف المقابل هو حساب `1300` (العملاء - مدينون)، ومربوط
    بـ`party_type/party_id` زي أي سند عادي.
  - "سيارة" → `4900` (وارد) أو `5800` (صادر).
  - "شركة"/"بنك" → `4900` (وارد) أو `5600` مصروفات إدارية (صادر) — دول
    فئتان عامتان مفيش لهم تصنيف تفصيلي حاليًا، فبنستخدم الحسابات العامة.
  - الرصيد الافتتاحي تحديدًا → مدين الصندوق (`1100`) / دائن `3200`
    (الأرباح المرحلة)، مش إيراد عادي، عشان ميضخمش أرقام الإيرادات.
- `updateTransactionInSupabase()`: بيحذف/يعكس السند القديم المرتبط ثم
  ينشئ سند جديد بالقيم المحدّثة (زي ما بيحصل في أي تعديل سند فعلي).
- `deleteTransaction()`: بيحذف/يعكس السند المرتبط (لو موجود) قبل ما يحذف
  صف `transactions` — فرصيد الحساب المحاسبي المقابل بيرجع صح.

## تحديث: زرار "تحويل إلى بنك" اتقفل كمان (نفس الجلسة)

القرار المتبع في أي نظام محاسبي: تحويل نقدي داخلي بين صندوق وبنك تابعين
لنفس الشركة **مش سند قبض/صرف** (السند بيكون مع طرف خارجي — عميل/مورد/مندوب)،
فاستخدام `transferCashToBank()` (قيد مباشر متوازن: مدين البنك / دائن الصندوق)
بدل سند كان أصلاً هو الصح — المشكلة الوحيدة كانت إن صف `transactions`
المصاحب للتحويل مكانش مربوط بالقيد، فحذفه كان بيسيب التحويل الفعلي معلّق.

**اللي اتعمل:**
- `accounting_schema_step4.sql` اتوسّع بعمودين إضافيين على `transactions`:
  `linked_journal_entry_id` و`linked_bank_account_id`.
- `Supabase.transferCashToBank()` بقى يرجّع `bank_account_id` مع القيد.
- دالة جديدة `Supabase.reverseCashToBankTransfer(journalEntryId, bankAccountId, amount)`
  بتحذف القيد وترجّع رصيد البنك لما كان عليه.
- `handleTransferSubmit()` في `treasury.html` بقى يعمل القيد الأول، ويسجّل
  صف `transactions` مربوط بيه.
- الحذف (`deleteTransaction`) بقى بيعكس التحويل صح لو الصف ده تحويل بنكي.
- **التعديل ممنوع** لصفوف التحويل من نافذة "تعديل المعاملة" العامة (مفيهاش
  اختيار بنك أصلاً) — لو غيّرت رأيك في تحويل، احذفه واعمل تحويل جديد
  بالقيمة الصح بدل التعديل.

## ملاحظات مهمة
1. **المعاملات القديمة** (اللي اتسجلت قبل الخطوة دي) معندهاش أي أعمدة ربط،
   فحذفها/تعديلها هيتجاهل عملية العكس بهدوء (مفيش حاجة أصلاً تتعكس) — أرقامها
   في `getTrialBalance()` هتفضل زي ما هي، محتاجة سكريبت تصحيح رجعي منفصل لو
   حصل عندك معاملات فعلية قبل كده (مش الحالة عندك دلوقتي).
2. فئتا "شركة" و"بنك" (في نافذة الإضافة اليدوية العادية، مش زرار التحويل)
   بيروحوا لنفس الحسابين العامين (`4900`/`5600`) حاليًا لأنه معندهمش تصنيف
   فرعي في دليل الحسابات — لو حابب تصنيف أدق قولّي وأضيفه بسهولة.

---

# تكملة تانية (نفس الجلسة) — تسديد العميل بقى سند حقيقي زي المورد بالظبط

## القرار
`suppliers.html` كان فيه أصلاً التطبيق الصح لتسديد طرف خارجي: زرار "تسديد"
بيعمل سند صرف حقيقي عبر `Supabase.addVoucher()` (مش بس يعدّل حقل `debt`
ويكتب سطر في `transactions`). `clients.html` كان بعيد عن النمط ده تمامًا —
زرار "تسديد" العميل كان بيعدّل `clients.debt` ويكتب سطر `transactions` بس،
من غير أي سند أو قيد. اتفقنا نوحّد العميل مع نفس نمط المورد بالظبط، على
3 خطوات صغيرة:

### خطوة 1 — واجهة فقط (بدون تغيير سلوك)
إضافة حقل "الاستلام في" (اختيار الصندوق `1100` أو مجموعة البنوك `1200`)
لنافذة "تسديد" في `clients.html`، وتحميل `cashAccounts` عند فتح الصفحة —
تمامًا زي `supplierPaymentCashAccount` في `suppliers.html`.

### خطوة 2 — الربط الفعلي بالمحرك المحاسبي
`handlePaymentSubmit()` بقى:
- بينشئ سند قبض حقيقي (`Supabase.addVoucher`): مدين = الحساب اللي
  المستخدم اختاره (صندوق/بنك)، دائن = حساب `1300` (العملاء - مدينون)،
  مربوط بـ `party_type: 'client'`.
- بعد كده يحدّث `clients.debt` زي ما كان بيعمل بالظبط (مفيش تغيير هنا).
- **الفرق المهم:** مبقاش بيكتب سطر في جدول `transactions` القديم خالص —
  زي `suppliers.html` بالظبط، اللي ملوش أي سطر `transactions` من الأساس.

### خطوة 3 — تصحيح تبعية مكسورة اكتشفتها أثناء التنفيذ
لقيت إن `client_statement.html` (كشف حساب العميل) كان بيعرض تاريخ الدفعات
عن طريق فلترة `transactions` بـ`category === 'عميل'` — يعني بعد خطوة 2،
أي تسديد جديد كان هيختفي من كشف حساب العميل تمامًا (رغم إنه اتسجل صح في
السند والقيد). صلّحتها بنفس اللي `supplier_statement.html` بيعمله بالظبط:
الصفحة بقت تجيب `Supabase.getVouchers()` وتفلترها بـ
`party_type === 'client' && party_id === clientId` بدل الاعتماد على
`transactions`.

## ملاحظات مهمة
1. **دفعات العميل القديمة** (اللي اتسجلت في `transactions` قبل التعديل
   ده) مش هتظهر في كشف الحساب الجديد لأنه بقى بيعرض السندات بس — مش مشكلة
   عندك دلوقتي لإنك لسه ماعملتش أي معاملات فعلية.
2. **فجوة معروفة برّه نطاق التعديل ده:** صفحات `reports.html` و`trips.html`
   و`settings.html` لسه بتحسب إجمالي الوارد/الصادر من جدول `transactions`
   القديم مباشرة — يعني أي رقم "إجمالي محصّل من العملاء" أو "صافي التدفق
   النقدي" في `reports.html` تحديدًا **مش هيشمل** تسديدات العملاء أو
   الموردين (لأنهم بقوا سندات، مش سطور `transactions`). `trips.html` عندها
   نفس القيد لكنه fallback موضّح بالكود نفسه، و`settings.html` بس بيعرض
   عدد صفوف كإحصائية بدون تأثير مالي حقيقي. توحيد `reports.html` تحديدًا
   محتاج إعادة هيكلة الصفحة كلها (دمج شكلين مختلفين من البيانات)، مش تعديل
   صغير — قرارك لو عايزها كخطوة منفصلة.

---

# تكملة تالتة (نفس الجلسة) — إيداع/سحب البنك (`banks.html`) بقى قيد حقيقي

## الاكتشاف
`banks.html` فيها نظام "إيداع/سحب" **مستقل تمامًا وموازي** لكل حاجة
اتصلحت قبل كده: بيعدّل `banks.balance` مباشرة (`Supabase.updateData('banks', ...)`)
من غير أي قيد محاسبي خالص، وبيحسب الرصيد عند التعديل/الحذف بإعادة تجميع
جدول `transactions` نفسه (`recalculateBankBalance`) — منفصل بالكامل عن
`chart_of_accounts`/`journal_entries`/`getTrialBalance()`. ده أخطر حاجة
لقيناها في المراجعة دي.

## اللي اتعمل (3 خطوات صغيرة)

### خطوة 1 — محرك محاسبي في `supabase.js` (مفيش تغيير سلوك)
- `transferBankToCash(bank, amount, description)` — عكس `transferCashToBank`
  الموجودة أصلاً: قيد حقيقي (مدين الصندوق 1100 / دائن حساب البنك) + تخفيض
  `banks.balance` عبر `adjustLinkedBankBalance`. بترجّع `bank_account_id`
  زي أختها بالظبط.
- `reverseBankToCashTransfer(journalEntryId, bankAccountId, amount)` —
  عكس عملية سحب (حذف القيد + رجوع الرصيد المسحوب).

### خطوة 2 — `handleTransactionSubmit()` (الإضافة الجديدة فقط)
إيداع/سحب جديد بقى ينادي `transferCashToBank`/`transferBankToCash` بدل
تعديل `balance` مباشرة، ويسجّل `linked_journal_entry_id` و
`linked_bank_account_id` على صف `transactions` (نفس عمودين خطوة 4 —
مفيش SQL جديد مطلوب).

### خطوة 3 — تعديل/حذف حركة بنكية
- `deleteBankTransaction()`: بقى يحذف القيد المحاسبي المرتبط (لو موجود)
  الأول، وبعدين يحذف صف `transactions` وينادي `recalculateBankBalance()`
  زي ما هو — ده بيدّي الرصيد النهائي الصح لأن كل حركة (قديمة أو جديدة)
  ليها صف مقابل في `transactions` دايمًا، فإعادة التجميع كافية طالما
  القيد اليتيم اتشال الأول.
- `editBankTransaction()`: بقى **يمنع** تعديل أي حركة مربوطة بقيد
  (`linked_journal_entry_id` موجود) ويوجّه المستخدم للحذف وعمل حركة
  جديدة بدل التعديل — نفس أسلوب "تحويل إلى بنك" في `treasury.html`
  بالظبط، لأن مفيش داعي محاسبي لتعديل مباشر لحركة مرتبطة بقيد.

## ملاحظات مهمة
1. حركات الإيداع/السحب **القديمة** (اللي اتعملت قبل التعديل ده، معندهاش
   `linked_journal_entry_id`) لسه ممكن تتعدل/تتحذف بالطريقة القديمة زي ما
   هي — بس هي أصلاً مش مربوطة بأي قيد محاسبي من الأساس، فمفيش حاجة تتعكس.
2. دلوقتي `banks.html`، `treasury.html`، `clients.html`، `suppliers.html`
   كلهم بيمروا على نفس المحرك المحاسبي (`addVoucher`/`transferCashToBank`/
   `transferBankToCash`) — القطعة الوحيدة الباقية هي `reports.html` (تقرير
   شامل لسه بيقرأ من `transactions` بس، محتاج إعادة هيكلة لو عايز تشمل
   السندات).

