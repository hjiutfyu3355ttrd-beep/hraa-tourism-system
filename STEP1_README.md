# الخطوة 1 من 4 — المحرك المحاسبي (دليل حسابات + قيود مزدوجة + سندات)

## إيه اللي اتعمل في الخطوة دي؟

1. **دليل حسابات محاسبي حقيقي** (أصول / خصوم / حقوق ملكية / إيرادات / مصروفات) بما فيها حسابات مصروفات الرحلات بمسميات العميل بالظبط: الانتقالات، الفنادق، الطيران، تأشيرات الوكيل، الباركود.
2. **قيود يومية مزدوجة (Debit/Credit)** — أي سند بيتسجل بينشئ قيد متوازن تلقائيًا.
3. **شاشة سندات جديدة (`vouchers.html`)**: سند قبض وسند صرف، بتختار منها الحساب النقدي (صندوق/بنك) والحساب المقابل من دليل الحسابات، والنظام بيرحّل القيد لوحده.
4. **3 قوائم مالية جديدة داخل صفحة التقارير (`reports.html`)** كتابات (Tabs): ميزان المراجعة، الأرباح والخسائر، المركز المالي — بتتحدث تلقائيًا من أي سند يتسجل.

## خطوات التشغيل (مهم جدًا قبل ما تجرب النظام)

1. افتح **Supabase Dashboard → SQL Editor**
2. لو أول مرة تشغل المشروع: شغّل ملف `database_schema.sql` الأساسي الأول (لو لسه ما شغلتوش)
3. بعد كده افتح ملف **`accounting_schema_step1.sql`** الجديد، انسخه بالكامل، الصقه في SQL Editor، واضغط **RUN**
4. تأكد إن 4 جداول جديدة ظهرت في Table Editor: `chart_of_accounts`, `journal_entries`, `journal_entry_lines`, `vouchers`
5. افتح `login.html` وسجّل دخولك، وهتلاقي رابط "**السندات**" جديد في القائمة الجانبية تحت "الإدارة المالية"

## للتجربة السريعة

1. من صفحة السندات: اعمل "سند قبض جديد" → اختار الصندوق كحساب نقدي، واختار "إيرادات الرحلات" كحساب مقابل، وسجّل مبلغ تجريبي
2. روح لصفحة "التقارير" → تبويب "ميزان المراجعة" → هتلاقي المبلغ ظهر تلقائيًا مقسّم مدين/دائن بشكل متوازن
3. جرّب تبويب "الأرباح والخسائر" هتلاقي المبلغ ظاهر كإيراد

## ملحوظة مهمة

الميزة دي (المحرك المحاسبي) هي أساس الخطوة 2 اللي جاية، واللي هنربط فيها كل حجز ورحلة ومصروف تلقائيًا بنفس القيود دي، فبعد ما تتأكد إن الخطوة دي شغالة تمام، نكمل عليها.
