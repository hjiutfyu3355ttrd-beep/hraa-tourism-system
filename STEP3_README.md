# خطوة 3 — ربط أرصدة البنوك تلقائيًا بالسندات ومصروفات الرحلة

## اللي اتعمل
تمت إضافة دالة `adjustLinkedBankBalance(accountId, delta)` في `supabase.js`، وتم ربطها في:

- `addVoucher()` — سند قبض يزيد رصيد البنك المرتبط، سند صرف ينقصه.
- `deleteVoucher()` — يعكس نفس الأثر بالظبط عند حذف السند.
- `addTripExpense()` — لو الدفع "نقدي" (مش "آجل")، ينقص رصيد البنك/الصندوق المختار.
- `deleteTripExpense()` — يرجّع المبلغ لو كان مصروف نقدي.

الدالة بتتأكد إن الحساب المحاسبي (`cash_account_id`) مرتبط فعليًا ببنك حقيقي عبر
`linked_bank_id` (من خطوة 2) قبل ما تحدّث `banks.balance`. لو الحساب مش مرتبط ببنك
(زي حساب صندوق نقدي عام مالوش بنك حقيقي مقابل)، الدالة متعملش حاجة.

فشل تحديث رصيد البنك (مثلاً مشكلة شبكة) بيتسجل تحذير في الـ console لكن **مبيوقفش**
حفظ السند/المصروف نفسه ولا القيد المحاسبي المرتبط به.

## متطلب أساسي
لازم تكون شغّلت `accounting_schema_step3.sql` على قاعدة بياناتك في Supabase قبل كده
(خطوة 2)، عشان عمود `linked_bank_id` يكون موجود وكل بنك يكون له حساب محاسبي مرتبط.

## اللي لسه محتاج انتباه
- لو عندك سندات أو مصروفات قديمة اتسجلت **قبل** هذا الإصلاح، أرصدة البنوك الحالية
  في جدول `banks` مش هتتصحح تلقائيًا بأثر رجعي — الإصلاح بيمنع تكرار المشكلة في أي
  عملية جديدة بس. لو حبيت تصحيح الأرصدة القديمة، يحتاج سكريبت SQL منفصل يعيد حساب
  كل رصيد بنك من مجموع حركاته الفعلية في `journal_entry_lines`.
