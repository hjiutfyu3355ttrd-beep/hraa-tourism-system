# خطوة 9 — بيان حركة البنك يعرض كل حاجة أثّرت فيه فعليًا

الخطة اتقسّمت 3 أجزاء صغيرة، كل جزء بيتنفذ بعد موافقة صريحة على اللي قبله.
الجزء ده (1) هو المُنفَّذ في هذا الملف.

## الجزء 1 — دمج السندات مع الحركات اليدوية في بيان الحركة (✅ منفّذ)

**المشكلة:** بعد إصلاح `recalculateBankBalance()` في خطوة 8 ليحسب الرصيد
من `journal_entry_lines` مباشرة، بقى الرصيد المعروض فوق بيان الحركة صحيح
100% — لكن الجدول تحته لسه بيعرض بس صفوف `transactions` فئة "بنك"
(إيداع/سحب يدوي). يعني أي بنك عليه تسديد عميل/مورد أو رصيد افتتاحي (من
خطوة 8) كان الرصيد فوق يتغيّر بينما الجدول تحته يفضل زي ما هو — تعارض
مربك للمستخدم حتى لو الرقم النهائي صح.

**الحل:** `loadBankTransactions()` بقت بتجيب مصدرين وتدمجهم في نفس
الجدول، مرتبين بالتاريخ:
- الحركات اليدوية القديمة (زي قبل كده بالظبط).
- كل سند (`vouchers`) حسابه النقدي (`cash_account_id`) هو حساب البنك ده
  تحديدًا — يشمل تسديدات العملاء/الموردين والرصيد الافتتاحي وأي سند عام
  اتعمل على البنك.

صفوف السندات بتتميّز ببادچ "سند" صغير، والبيان بيوضّح رقم السند
والطرف المرتبط (عميل/مورد). **بدون أزرار تعديل/حذف** على صفوف السندات —
عمدًا: تعديلها لازم يعدّي من الصفحة الأصلية بتاعتها (vouchers.html أو
clients.html أو suppliers.html) عشان مايحصلش تعارض بين مصدرين مختلفين
بيعدّلوا نفس القيد.

## الجزء 2 — رابط سريع من كل سند لمكان تعديله الصح (✅ منفّذ)

بدل عمود "من صفحة السندات" النصي بس، كل صف سند في بيان الحركة بقى فيه
رابط فعلي يودّي المستخدم على طول للمكان الصح:

- سند تسديد عميل → `client_statement.html?client_id=<id>` (كشف حساب
  العميل ده تحديدًا، متفتوح جاهز من غير ما تدوّر عليه من القايمة المنسدلة).
- سند تسديد مورد → `supplier_statement.html?supplier_id=<id>` (نفس الفكرة).
- أي سند عام تاني → `vouchers.html` (من غير تمييز سند بعينه، لعدم وجود
  بحث/فلترة بالرقم في الصفحة دي أصلًا).

عشان ده يشتغل، `client_statement.html` و `supplier_statement.html` بقى
كل واحد فيهم بيقرأ باراميتر URL (`?client_id=...` / `?supplier_id=...`)
عند فتح الصفحة، ولو موجود بيفتح كشف الحساب بتاع الطرف ده تلقائيًا بدل
ما يسيب المستخدم يختاره يدويًا من القائمة.

## الجزء 3 المحتمل بعد كده

مراجعة سريعة على `client_statement.html` و`supplier_statement.html` و
`trip_statement.html` للتأكد إنهم كمان بيعرضوا كل الحركات اللي فعليًا
بتأثّر على أرصدتهم (مش بس السندات المباشرة)، بنفس منطق الدمج ده — كخطوة
اتساق أخيرة قبل الرجوع لموضوع `reports.html` الأكبر.

