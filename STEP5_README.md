# خطوة 5 (من مراجعة التكامل) — إصلاح صفحة إدارة المستخدمين

## المشكلة
`users.html` كانت بتنادي `Supabase.addUser()`، `Supabase.updateUser()`، `Supabase.deleteUser()` —
الثلاثة دول **مش موجودين خالص** في `supabase.js`. يعني أي محاولة إضافة/تعديل/حذف
مستخدم من الواجهة كانت بترمي error فورًا (bug حقيقي مش مجرد تكامل).

## الحل
أضفت الدوال الثلاثة في `supabase.js`:

- **`addUser(email, password, fullName, role)`** — نفس آلية `register.html` بالظبط:
  إنشاء حساب حقيقي في Supabase Auth عبر `/auth/v1/signup`، وبعدين إضافة صف مقابل
  في `public.users` (باستخدام جلسة المدير الحالي).
- **`updateUser(id, data)`** — تحديث `full_name` / `role` / `is_active` في `public.users`.
- **`deleteUser(id)`** — حذف صف المستخدم من `public.users`.

## قيد مهم لازم تعرفه (مش حاجة أقدر أتخطاها من غير باك إند)
التطبيق ده front-end بحت ومعندوش **Supabase Service Role Key** (مفتاح إداري).
من غيره:

1. **مينفعش أغيّر كلمة مرور مستخدم تاني.** لو حاولت من صفحة "تعديل مستخدم"،
   هترجع رسالة خطأ واضحة بدل ما تفشل بصمت — عشان كده خبّيت حقل كلمة المرور
   تمامًا في وضع التعديل وحطيت ملاحظة مكانه إن المستخدم يغيّرها بنفسه من صفحة
   "الملف الشخصي".
2. **"حذف" مستخدم بيمسح صف بياناته من `public.users` بس** — مش حساب الـ Auth
   بتاعه فعليًا. لو كان عنده جلسة (token) شغالة، هيقدر يفضل داخل النظام لحد
   ما الجلسة تنتهي، لكن من غير اسم/دور. البديل الأنضف لو عايز تمنع دخوله فورًا
   هو استخدام "تعديل" وتخلي حالته غير مفعّل (`is_active: false`) بدل الحذف
   الكامل.
3. لو مفعّل عندك في إعدادات Supabase "تأكيد البريد الإلكتروني الإجباري"، أي
   مستخدم جديد هتضيفه من الصفحة دي هيحتاج يفتح إيميله ويأكّد الحساب قبل ما
   يقدر يسجّل دخول — ده سلوك Supabase نفسه.

لو حبيت حل نهائي (تغيير كلمة مرور أي حد، وحذف فعلي لحساب الـ Auth)، محتاج
Edge Function أو باك إند صغير يستخدم Service Role Key بأمان (السر ده خطير
جدًا يتحط في كود الواجهة مباشرة). قولّي لو عايز أجهزلك خطوات عمل ده.
