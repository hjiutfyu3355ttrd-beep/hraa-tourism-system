# الخطوة 2 من 4 — ربط العمليات بالمحاسبة (تسليم)

## 1) أول خطوة: شغّل ملف SQL الجديد
افتح Supabase Dashboard → SQL Editor → New query، والصق **محتوى ملف
`accounting_schema_step2.sql` بالكامل**، ثم Run.

يضيف الملف:
- جدول `suppliers` (الموردين)
- جدول `trip_expenses` (تكاليف الرحلة المُصنّفة)
- حسابين ناقصين في دليل الحسابات: **أوفر باركود (5510)** و **باركود الغرفة (5520)**
- عمودين اختياريين على جدول `vouchers`: `trip_id` و `booking_id` للربط والتتبع

تأكد بعدها من ظهور `suppliers` و `trip_expenses` في Table Editor.

## 2) الصفحات الجديدة
| الصفحة | الوظيفة |
|---|---|
| `suppliers.html` | إضافة/حذف موردين + تسديد لهم (سند صرف تلقائي) |
| `supplier_statement.html` | كشف حساب كل مورد (مشتريات آجلة + سداد + رصيد) |
| `trip_details.html` | **الصفحة اللي كانت مفقودة تمامًا** رغم إن `trips.html` بيحاول يفتحها. فيها تكاليف الرحلة السبعة بالظبط بمسميات شيتك (الانتقالات، الفنادق، الطيران، تأشيرات الوكيل، الباركود، أوفر باركود، باركود الغرفة) + أرباح الرحلة الفعلية |

## 3) الصفحات المُعدّلة
- **`bookings.html`**: أي دفعة (مقدمة عند إنشاء الحجز، أو دفعة لاحقة عبر زرار "تسجيل دفعة") بقت بتنشئ **سند قبض تلقائي** يترحّل مباشرة لميزان المراجعة والأرباح والخسائر — مش مجرد رقم بيتخزن في عمود.
- **`trips.html`**: الإيراد والمصروف وصافي الربح بقوا بيحسبوا من **الحجوزات الفعلية + تكاليف الرحلة المُصنّفة الجديدة**، مش بس من الجدول القديم اليدوي.
- **باقي الصفحات**: أُضيف رابط "الموردين" و"كشف حساب المورد" في القائمة الجانبية.
- **`supabase.js`**: أُضيفت دوال `getSuppliers/addSupplier/...`، `addTripExpense`، `recordBookingPayment`، `getTripFinancials`، إلخ — كلها بترحّل قيود مزدوجة متوازنة تلقائيًا.

## 4) سيناريو اختبار كامل (افعله بالترتيب)
1. **مورد جديد**: `suppliers.html` → أضف مورد "شركة النقل الذهبي".
2. **رحلة + حجز**: من `trips.html` أنشئ رحلة، ثم من `bookings.html` اعمل حجز لعميل على هذه الرحلة بدفعة مقدمة 1000 → تأكد من ظهور سند قبض جديد في `vouchers.html`.
3. **تكلفة رحلة نقدية**: من `trips.html` اضغط "التفاصيل" على الرحلة (تفتح `trip_details.html` لأول مرة وتشتغل) → أضف تكلفة "الانتقالات" 300 جنيه دفع نقدي من الصندوق.
4. **تكلفة رحلة آجلة على مورد**: أضف تكلفة "الفنادق" 2000 جنيه دفع آجل على المورد اللي أضفته.
5. تأكد إن **صافي ربح الرحلة** في `trip_details.html` اتحدث فورًا (1000 إيراد − 2300 مصروف).
6. روح `reports.html` → ميزان المراجعة → لازم يظهر متوازن ويشمل كل الحركات دي.
7. روح `supplier_statement.html` → اختر المورد → لازم تشوف الـ 2000 جنيه آجلة ظاهرة كمستحق عليه.
8. من `suppliers.html` سدّد للمورد 2000 جنيه → تأكد إن المستحق رجع صفر وظهر سند صرف جديد.

## 5) اللي لسه مجدول (الخطوة 3 و4)
- تصدير/استيراد إكسيل فعلي (مش زرار شكلي)
- تصدير PDF فعلي في التقارير
- المراجعة النهائية + تنظيف الكود + تعليمات مختصرة للعميل
