# خطوة 6 (من مراجعة التكامل) — تحويل الخزينة→بنك بقى قيد محاسبي حقيقي

## المشكلة اللي اتصلحت
زرار "تحويل من الخزينة إلى بنك" في `treasury.html` كان بيعدّل `banks.balance`
مباشرة (`Supabase.updateData('banks', ...)`) من غير أي قيد محاسبي — يعني رصيد
البنك كان بينفصل بصمت عن `getTrialBalance()` وعن الأرصدة اللي بقت بتتحدّث
تلقائيًا من السندات (خطوة 3).

## الحل
دالة جديدة `Supabase.transferCashToBank(bank, amount, description)`:
1. بتعمل **قيد يومية حقيقي متوازن**: مدين = الحساب المحاسبي المرتبط بالبنك،
   دائن = حساب "الصندوق" (1100) — فبيبان صح في ميزان المراجعة.
2. بتحدّث `banks.balance` فعليًا من خلال `adjustLinkedBankBalance` (نفس آلية
   السندات بالظبط)، مش بتعديل مباشر على الرقم.

`treasury.html` بقت تنادي الدالة دي بدل التعديل المباشر، مع الاحتفاظ بتسجيل
الحركة في جدول `transactions` القديم زي ما هو (عشان رقم "رصيد الخزينة
النقدية" المعروض في الصفحة يفضل صحيح لحد ما نقرر نوحّد الصفحة بالكامل).

## اللي لسه فاضل (نطاق أوسع، محتاج قرارك)
المعاملات اليدوية العادية (زرار "إضافة وارد/صادر" في treasury.html، مش
التحويل للبنك) **لسه** بتتسجل في جدول `transactions` القديم بس، من غير قيد
محاسبي مقابل. يعني "رصيد الخزينة النقدية" المعروض في الصفحة لسه رقم منفصل عن
ميزان المراجعة الحقيقي. توحيد الجزء ده محتاج قرار منك: نخلي كل حركة يدوية
هناك تعمل سند تلقائي زي السندات (`vouchers.html`)، ولا نوقف الإدخال اليدوي
من treasury.html كليًا ونوجّه المستخدم لصفحة السندات مباشرة؟
