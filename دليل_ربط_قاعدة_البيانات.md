# دليل ربط مشروع Supabase الجديد بالنظام

## الخطوة ١: احصل على بيانات الاتصال
من لوحة تحكم Supabase لمشروعك الجديد:
`Project Settings` → `API`
انسخ منها قيمتين:
- **Project URL** (شكلها: `https://xxxxxxxxxxxx.supabase.co`)
- **anon / public key** (مفتاح طويل يبدأ عادة بـ `sb_publishable_...` أو `eyJ...`)

## الخطوة ٢: حدّث الكود (مكان واحد بس دلوقتي)
افتح ملف **`supabase.js`** وعدّل أول 10 أسطر فيه:

```js
var SUPABASE_CONFIG = {
    URL: 'ضع هنا Project URL بتاعك',
    KEY: 'ضع هنا anon public key بتاعك'
};
```

كنا قبل كده بنلاقي نفس القيمتين متكررة يدويًا جوه `login.html` و `register.html`
لوحدهم — تم تصليح الاثنين علشان يقرأوا من نفس المكان ده تلقائيًا، فمفيش داعي
تلمسهم تاني في أي تحديث مستقبلي.

## الخطوة ٣: أنشئ الجداول
1. من لوحة Supabase: `SQL Editor` → `New query`
2. افتح ملف **`database_schema.sql`** المرفق، انسخه بالكامل، والصقه هناك
3. اضغط **RUN** (مرة واحدة كفاية)
4. تأكد من `Table Editor` إن كل الجداول دي ظهرت:
   `users`, `user_settings`, `clients`, `trips`, `representatives`,
   `rep_transactions`, `bookings`, `banks`, `transactions`, `services`,
   `support_messages`, `hotels`, `cars`

الملف نفسه بيفعّل الحماية (RLS) وبيضبط الصلاحيات المناسبة لكل جدول، وبينشئ
دالة `get_users_count()` اللي صفحة التسجيل محتاجاها.

## الخطوة ٤: إعداد مهم في صفحة الـ Authentication
من `Authentication` → `Sign In / Providers` (أو `Authentication` → `Settings`
حسب واجهة Supabase عندك) دوّر على خيار **Confirm email**:

- لو سبته **مفعّل** (الوضع الافتراضي): أي مستخدم يسجّل هيحتاج يفتح إيميله
  ويأكد الحساب الأول قبل ما يقدر يسجّل دخول.
- الأفضل لنظام داخلي زي ده إنك **تعطّله** (Disable) عشان أول ما حد يعمل
  تسجيل يقدر يدخل على طول من غير ما يستنى تأكيد إيميل.

## الخطوة ٥: سجّل أول حساب (هيبقى مدير تلقائيًا)
افتح `register.html` — أول شخص يسجّل في المشروع الجديد هيبان له شارة
"👑 المستخدم الأول - سيكون مديراً" وهيتسجل بصلاحية admin تلقائيًا (لأن
`get_users_count()` هترجّع صفر). بعد كده أي حد تاني يسجّل هيبقى `user` عادي.

## الخطوة ٦: جرّب تسجيل الدخول
افتح `login.html` وسجّل بنفس البيانات. لو دخلت على الداشبورد يبقى الربط تمام.

---

## ملاحظة مهمة عن صفحة إدارة المستخدمين (users.html)

أثناء المراجعة لقيت إن الأزرار الخاصة بـ **إضافة / تعديل / حذف مستخدم آخر**
(من صفحة `users.html`) بتنادي على دوال (`addUser`, `updateUser`, `deleteUser`)
مش موجودة أصلاً في `supabase.js` — يعني حاليًا الأزرار دي مش هتشتغل.

**السبب:** عمليات زي حذف حساب شخص تاني أو تغيير كلمة سره تحتاج صلاحية
"Service Role" من Supabase، والمفتاح ده **خطر جدًا نحطه في كود المتصفح**
(أي حد يقدر يفتح Developer Tools ويشوفه ويتحكم في كل حسابات النظام). عشان
كده مفيش حل آمن بمجرد تعديل الملفات اللي عندنا.

**الحل الصح:** إنشاء "Edge Function" بسيطة على Supabase (كود سيرفر منفصل
يشتغل بمفتاح الـ service role بأمان) تستقبل الطلب من الواجهة وتنفذ عملية
الحذف/التعديل بدل ما الكود يعمل ده مباشرة. لو حابب أعملها لك قولّي وهعملها
في خطوة منفصلة.

**الحل المؤقت:** ممكن تدير المستخدمين يدويًا من `Authentication` → `Users`
جوه لوحة تحكم Supabase نفسها لحد ما نعمل الحل الدائم.

أما تعديل **بياناتك انت نفسك** (الاسم، رقم التليفون، كلمة السر) من صفحة
`profile.html` فده تم تصليحه وبيشتغل عادي، لأنه أمن (كل حد بيعدل بياناته هو
بس).
