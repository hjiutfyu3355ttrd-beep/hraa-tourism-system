# خطوة 8 — إغلاق ثغرات: رصيد البنك المباشر + الأرصدة الافتتاحية للبنك/العميل/المورد

لا يوجد ملف SQL جديد في هذه الخطوة — كل الإصلاحات على مستوى الكود فقط
(supabase.js + banks.html + clients.html + suppliers.html)، بدون أي عمود
أو جدول جديد.

## 1) banks.html — تعديل بيانات بنك مبقاش بيغيّر الرصيد مباشرة

نافذة "تعديل البنك" كان فيها حقل "الرصيد الافتتاحي" قابل للتعديل بحرية،
وأي رقم تكتبه فيه يتسجل مباشرة على `banks.balance` من غير أي قيد محاسبي —
يعني كنت تقدر "تزوّد" رصيد بنك من غير أي أثر في ميزان المراجعة.

- عند التعديل: الحقل بقى معطّل (disabled) للعرض بس، مع تلميح توضيحي تحته.
- `updateBank()` بقى مبعتش `balance` أصلًا في التحديث.
- أي تغيير فعلي في رصيد بنك موجود لازم يعدّي من زرار "إيداع/سحب" (زي
  قبل كده بالظبط)، عشان يتسجل بقيد يومية حقيقي.

## 2) رصيد افتتاحي لبنك/عميل/مورد جديد بقى بيتسجل بقيد محاسبي حقيقي

قبل كده: بنك جديد برصيد افتتاحي، أو عميل جديد بمديونية افتتاحية، أو مورد
جديد بمستحق افتتاحي — كانوا بيتسجلوا بس في جدول البنك/العميل/المورد نفسه
(`balance`/`debt`)، من غير أي قيد يعدّي على `journal_entry_lines`. النتيجة:
ميزان المراجعة (`getTrialBalance`) كان يفضل ناقص المبالغ دي تمامًا، لحد ما
حد يعمل حركة تانية تلمس نفس الحساب.

الحل — الثلاثة بقوا بينشئوا قيد افتتاحي حقيقي عبر `Supabase.addVoucher()`
(نفس بالظبط منطق الرصيد الافتتاحي للخزينة في `treasury.html`):

| الحالة                      | مدين              | دائن              |
|------------------------------|-------------------|-------------------|
| بنك جديد برصيد افتتاحي        | حساب البنك (12xx) | 3200 الأرباح المرحلة |
| عميل جديد بمديونية افتتاحية   | 1300 العملاء       | 3200 الأرباح المرحلة |
| مورد جديد بمستحق افتتاحي      | 3200 الأرباح المرحلة | 2100 الموردون      |

- `banks.html` → `addBank()`: البنك بيتسجل برصيد `0` الأول، وبعدين لو فيه
  رصيد افتتاحي بيتعمله سند حقيقي بيرفع `banks.balance` عبر
  `adjustLinkedBankBalance` (نفس آلية أي سند عادي) — مش تعيين مباشر.
- `clients.html` → `handleClientSubmit()`: لو `debt > 0` وقت الإضافة،
  بيتعمل سند صرف (مدين 1300 / دائن 3200) مرتبط بالعميل (`party_id`).
- `suppliers.html` → `handleSupplierSubmit()`: لو `debt > 0` وقت الإضافة،
  بيتعمل سند قبض (مدين 3200 / دائن 2100) مرتبط بالمورد (`party_id`).

كل الحالات الثلاثة: لو حساب 3200/1300/2100 مش موجود في دليل الحسابات
(يعني الـ migrations الأساسية متشغلتش)، العملية الأساسية (إضافة البنك/
العميل/المورد) بتكمل عادي والتحذير بيتسجل في الـ console بس — مفيش
سلوك بيوقف المستخدم.

## 3) الأخطر: `recalculateBankBalance` في banks.html كان ممكن يمسح رصيد بنك

`recalculateBankBalance()` القديمة كانت بتعيد حساب رصيد البنك بجمع صفوف
`transactions` (فئة "بنك" + `reference` = اسم البنك) بس — يعني مفيش وعي
خالص بالسندات (تسديد عميل/مورد عبر البنك ده) أو مصروفات الرحلات أو حتى
الرصيد الافتتاحي الجديد من الخطوة دي. الدالة دي بتتنادى تلقائيًا بعد أي
حذف أو تعديل حركة إيداع/سحب — فأي بنك عليه نشاط سندات، أول حذف/تعديل
حركة إيداع/سحب كان هيصفّر أو يغلط رصيده الحقيقي بالكامل.

الحل: أضفنا `Supabase.getBankAccountBalance(bankId)` في `supabase.js` —
بتحسب الرصيد مباشرة من `journal_entry_lines` للحساب المحاسبي المرتبط
بالبنك (مدين - دائن)، يعني بتاخد في الاعتبار كل حاجة أثّرت عليه فعليًا.
`recalculateBankBalance()` في `banks.html` بقت بتنادي الدالة دي بدل الحساب
اليدوي القديم من `transactions`.

## نقطة كانت مفتوحة — اتقفلت في خطوة 9 (جزء 1)

`loadBankTransactions()` بقت بتدمج السندات (تسديد عميل/مورد، رصيد
افتتاحي، أي سند قبض/صرف اتعمل على حساب البنك ده) مع حركات الإيداع/السحب
اليدوية القديمة في نفس بيان الحركة، بشارة "سند" مميّزة، وبدون أزرار
تعديل/حذف على صفوف السندات (التعديل/الحذف بتاعها لازم يعدّي من
vouchers.html/clients.html/suppliers.html، مش من هنا). بكده مجموع الصفوف
المعروضة بقى مطابق للرصيد المحسوب فوق (`getBankAccountBalance`).

راجع STEP9_README.md لتفاصيل باقي خطوة 9.

